---
name: Link Check
on:
  workflow_dispatch:

jobs:
  link_check:
    runs-on: ubuntu-latest
    name: Link Check
    steps:
      - uses: filiph/linkcheck-action@v1
        with:
          arguments: https://ebwiki.org
